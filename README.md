# Телеграм-бот "Инга" для FAQ по стажировкам

Этот проект представляет собой Telegram-бота, который отвечает на часто задаваемые вопросы о стажировках в компании "Ингосстрах".

## 🚀 Установка и запуск

Для запуска проекта на вашем компьютере, следуйте этой пошаговой инструкции.

### 1. Установите Python

Если у вас еще не установлен Python, скачайте и установите его с официального сайта.

-   **[Скачать Python](https://www.python.org/downloads/)**

**Важно:** Во время установки обязательно поставьте галочку **"Add Python to PATH"**.

![Add Python to PATH](https://docs.python.org/3/_images/win_installer.png)

### 2. Клонируйте репозиторий

Откройте терминал (командную строку) и выполните следующую команду, чтобы скопировать проект на ваш компьютер:

```bash
git clone https://github.com/ваш-логин/ваш-репозиторий.git
cd ваш-репозиторий
```
*(Замените `ваш-логин/ваш-репозиторий` на реальный URL вашего репозитория)*

### 3. Создайте и активируйте виртуальное окружение

Виртуальное окружение необходимо, чтобы изолировать зависимости проекта от системных.

```bash
# Создаем виртуальное окружение в папке "venv"
python -m venv venv

# Активируем его:
# Windows
.\venv\Scripts\activate
# macOS / Linux
source venv/bin/activate
```
После активации в начале строки терминала вы должны увидеть `(venv)`.

### 4. Установите зависимости

Выполните команду для установки всех необходимых библиотек из файла `requirements.txt`:

```bash
pip install -r requirements.txt
```

### 5. Настройте токен бота

Создайте в корневой папке проекта файл с именем `.env` и добавьте в него токен вашего бота.

1.  Создайте файл `.env`.
2.  Откройте его в текстовом редакторе и добавьте следующую строку:

```
TELEGRAM_TOKEN=ВАШ_ТЕЛЕГРАМ_ТОКЕН
```
Замените `ВАШ_ТЕЛЕГРАМ_ТОКЕН` на реальный токен, который вы получили от [@BotFather](https://t.me/BotFather).

### 6. Запустите бота

Теперь, когда все готово, запустите бота командой:

```bash
python bot.py
```

Если все сделано правильно, в терминале появится сообщение о том, что бот запущен.

### 7. Как остановить бота

Чтобы остановить работу бота, просто нажмите `Ctrl + C` в терминале, где он запущен.

---

## 🏗️ Структура проекта

Проект имеет простую и модульную структуру, где каждый файл отвечает за свою конкретную задачу:

-   `bot.py`: **Главный файл бота.**
    -   Содержит основную логику приложения: запуск, настройку `ConversationHandler` (менеджера диалогов) и всех обработчиков команд и кнопок (`/start`, "Главное меню" и т.д.).

-   `keyboards_and_data.py`: **Клавиатуры и данные.**
    -   Отвечает за загрузку данных из `faq_data.json`.
    -   Содержит функции для динамического создания всех `inline`-клавиатур (главное меню, меню с вопросами).

-   `config.py`: **Конфигурация.**
    -   Загружает и предоставляет доступ к переменным окружения (например, к токену бота из `.env` файла).

-   `tracking.py`: **Отслеживание.**
    -   Реализует простую логику для подсчета количества переходов на лендинг. Результат сохраняется в файл `landing_clicks.txt`.

-   `faq_data.json`: **База данных FAQ.**
    -   Файл в формате JSON, который хранит все категории, вопросы (полные и короткие), а также ответы на них. Легко редактируется без изменения кода.

-   `requirements.txt`: **Зависимости.**
    -   Список всех библиотек Python, необходимых для работы проекта.

-   `.env`: **Секретные данные.**
    -   Локальный файл для хранения секретных данных, таких как `TELEGRAM_TOKEN`. **Не должен** попадать в систему контроля версий Git.

